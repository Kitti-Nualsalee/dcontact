{
  "name": "@d-contact/ui",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js",
    "./tokens.css": "./src/tokens.css",
    "./tailwind-preset": "./src/tailwind-preset.cjs"
  },
  "files": ["dist", "src/tokens.css", "src/tailwind-preset.cjs"],
  "scripts": {
    "generate": "node scripts/generate-tokens.mjs",
    "build": "pnpm generate && tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node scripts/check-contrast.mjs"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
